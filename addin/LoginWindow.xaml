<Window x:Class="VH_IFC_QR.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Inloggen - VH Engineering"
        Height="390" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        FontFamily="Segoe UI"
        MouseDown="Window_MouseDown"
        KeyDown="Window_KeyDown">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="5" GlassFrameThickness="0" CornerRadius="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <!-- Style for Rounded Buttons -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                             <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2980B9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                         <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                         <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#FFFFFF" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="12" UseLayoutRounding="True" SnapsToDevicePixels="True" Margin="15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Direction="270" RenderingBias="Quality" ShadowDepth="3" Opacity="0.15"/>
        </Border.Effect>
        <Grid Margin="24,16">
            <ScrollViewer VerticalScrollBarVisibility="Auto" VerticalAlignment="Center">
                <StackPanel Margin="0,4">
                    <TextBlock Text="VH Engineering" FontSize="20" FontWeight="Bold" Foreground="#111827" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                    <TextBlock Text="Inloggen om te exporteren" FontSize="13" Foreground="#6B7280" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                    <TextBlock Text="Gebruikersnaam" FontWeight="SemiBold" Foreground="#374151" FontSize="13" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtUsername" Height="38" Margin="0,0,0,12"/>

                    <TextBlock Text="Wachtwoord" FontWeight="SemiBold" Foreground="#374151" FontSize="13" Margin="0,0,0,5"/>
                    <PasswordBox x:Name="txtPassword" Height="38" Margin="0,0,0,18"/>

                    <Button x:Name="btnLogin" Content="Inloggen" Height="42" Click="BtnLogin_Click" IsDefault="True" FontSize="14">
                        <Button.Effect>
                            <DropShadowEffect Color="#3498DB" Direction="270" ShadowDepth="2" Opacity="0.3" BlurRadius="8"/>
                        </Button.Effect>
                    </Button>

                    <TextBlock x:Name="lblStatus" Text="" Foreground="#DC2626" HorizontalAlignment="Center" Margin="0,12,0,0" TextWrapping="Wrap" FontSize="12" FontWeight="SemiBold"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
