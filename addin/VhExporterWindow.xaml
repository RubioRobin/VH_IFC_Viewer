<Window x:Class="VH_IFC_QR.VhExporterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:VH_IFC_QR"
        Title="VH Engineering - IFC export"
        Width="780"
        Height="700"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        FontFamily="Segoe UI"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        PreviewKeyDown="Window_PreviewKeyDown">

    <Window.Resources>
        <!-- Officiële primaire en accentkleur van vh-engineering.nl. -->
        <SolidColorBrush x:Key="VhGoldBrush" Color="#C3A875"/>
        <SolidColorBrush x:Key="VhGoldHoverBrush" Color="#806D49"/>
        <SolidColorBrush x:Key="VhInkBrush" Color="#000000"/>
        <SolidColorBrush x:Key="VhMutedBrush" Color="#64748B"/>
        <SolidColorBrush x:Key="VhLineBrush" Color="#DCE3ED"/>
        <local:PhaseIfcCountConverter x:Key="PhaseIfcCountConverter"/>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="Background" Value="{StaticResource VhGoldBrush}"/>
            <Setter Property="Foreground" Value="#172033"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="buttonBorder" Background="{TemplateBinding Background}" CornerRadius="9">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="buttonBorder" Property="Background" Value="{StaticResource VhGoldHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="buttonBorder" Property="Opacity" Value="0.82"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="buttonBorder" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="18,0"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#334155"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderBrush" Value="{StaticResource VhLineBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="buttonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="9">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="buttonBorder" Property="Background" Value="#F8FAFC"/>
                                <Setter TargetName="buttonBorder" Property="BorderBrush" Value="#B9C5D4"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="buttonBorder" Property="Background" Value="#EEF2F6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IconButtonStyle" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="Visibility" Value="Collapsed"/>
        </Style>

        <Style x:Key="TextButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#806D49"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="buttonBorder" Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="buttonBorder" Property="Background" Value="#F3EFE5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#334155"/>
            <Setter Property="Margin" Value="0,0,0,7"/>
        </Style>

        <Style x:Key="RoundedTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="#172033"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="{StaticResource VhLineBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="9">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="12,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource VhGoldBrush}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsReadOnly" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F8FAFC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#172033"/>
            <Setter Property="BorderBrush" Value="{StaticResource VhLineBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="toggleButton"
                                          Focusable="False"
                                          ClickMode="Press"
                                          Background="White"
                                          BorderBrush="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                          BorderThickness="{Binding BorderThickness, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="border"
                                                Background="White"
                                                BorderBrush="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                BorderThickness="{Binding BorderThickness, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                CornerRadius="9">
                                            <Grid Margin="{Binding Padding, RelativeSource={RelativeSource AncestorType=ComboBox}}">
                                                <ContentPresenter VerticalAlignment="Center"
                                                                  Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                                  ContentTemplate="{Binding SelectionBoxItemTemplate, RelativeSource={RelativeSource AncestorType=ComboBox}}"/>
                                                <Path HorizontalAlignment="Right" VerticalAlignment="Center" Fill="#64748B"
                                                      Data="M 0 0 L 8 0 L 4 5 Z"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="BorderBrush" Value="#B9C5D4"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource VhGoldBrush}"/>
                                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   PopupAnimation="Slide">
                                <Border MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                        Margin="0,4,0,0"
                                        Background="White"
                                        BorderBrush="{StaticResource VhLineBrush}"
                                        BorderThickness="1"
                                        CornerRadius="9">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="12" ShadowDepth="3" Opacity="0.16"/>
                                    </Border.Effect>
                                    <ScrollViewer MaxHeight="220" Padding="4" CanContentScroll="True">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="itemBorder" Padding="{TemplateBinding Padding}" CornerRadius="6">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="itemBorder" Property="Background" Value="#F3EFE5"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="itemBorder" Property="Background" Value="#EEE5D2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PhaseCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Border x:Name="card" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="12,7">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="18"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border x:Name="checkMark" Width="16" Height="16" CornerRadius="3" BorderBrush="#AAB8C8" BorderThickness="1" VerticalAlignment="Center">
                                    <Path x:Name="checkIcon" Data="M 3 8 L 6.5 11.5 L 12.5 4.5" Stroke="White" StrokeThickness="1.8" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Visibility="Collapsed"/>
                                </Border>
                                <ContentPresenter Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center" TextElement.Foreground="#334155" TextElement.FontSize="14"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="card" Property="Background" Value="#F8FAFC"/>
                                <Setter TargetName="card" Property="BorderBrush" Value="#B9C5D4"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="card" Property="Background" Value="#FBF8F1"/>
                                <Setter TargetName="card" Property="BorderBrush" Value="{StaticResource VhGoldBrush}"/>
                                <Setter TargetName="checkMark" Property="Background" Value="{StaticResource VhGoldBrush}"/>
                                <Setter TargetName="checkMark" Property="BorderBrush" Value="{StaticResource VhGoldBrush}"/>
                                <Setter TargetName="checkIcon" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="card" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="OptionCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#334155"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Border Background="Transparent" Padding="2">
                            <StackPanel Orientation="Horizontal">
                                <Border x:Name="checkMark" Width="16" Height="16" CornerRadius="3" BorderBrush="#AAB8C8" BorderThickness="1" Margin="0,0,8,0">
                                    <Path x:Name="checkIcon" Data="M 3 8 L 6.5 11.5 L 12.5 4.5" Stroke="White" StrokeThickness="1.8" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Visibility="Collapsed"/>
                                </Border>
                                <ContentPresenter VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="checkMark" Property="Background" Value="{StaticResource VhGoldBrush}"/>
                                <Setter TargetName="checkMark" Property="BorderBrush" Value="{StaticResource VhGoldBrush}"/>
                                <Setter TargetName="checkIcon" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="5" GlassFrameThickness="0" CornerRadius="0"/>
    </WindowChrome.WindowChrome>

    <Border Margin="15" Background="#FFFFFF" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="14">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Direction="270" ShadowDepth="4" Opacity="0.17"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1" CornerRadius="13,13,0,0" Padding="28,14">
                <Grid MouseLeftButtonDown="Header_MouseDown">
                    <StackPanel>
                        <TextBlock Text="IFC Assembly Export" Foreground="#000000" FontSize="21" FontWeight="Bold"/>
                    </StackPanel>
                    <Button Style="{StaticResource IconButtonStyle}" Content="×" HorizontalAlignment="Right" VerticalAlignment="Top" Click="Close_Click" IsCancel="True"/>
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
                <Grid Margin="28,24">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Style="{StaticResource FieldLabelStyle}" Text="IFC-versie"/>
                            <ComboBox Style="{StaticResource RoundedComboBoxStyle}"
                                      ItemsSource="{Binding IfcVersions}"
                                      SelectedItem="{Binding SelectedIfcVersion, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Style="{StaticResource FieldLabelStyle}" Text="Coördinatenbasis"/>
                            <ComboBox Style="{StaticResource RoundedComboBoxStyle}"
                                      ItemsSource="{Binding CoordinateBaseOptions}"
                                      SelectedItem="{Binding SelectedCoordinateBase, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                    </Grid>

                    <Border Grid.Row="1" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="10" Padding="16" Margin="0,0,0,20">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Style="{StaticResource FieldLabelStyle}" Text="Exportmap" Margin="0,0,0,7"/>
                                <TextBox Style="{StaticResource RoundedTextBoxStyle}"
                                         Text="{Binding FileExportPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                            <Button Grid.Column="1" Style="{StaticResource SecondaryButtonStyle}" Content="Bladeren" Command="{Binding BrowseCommand}" Margin="12,20,0,0"/>
                        </Grid>
                    </Border>

                    <Grid Grid.Row="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="230"/>
                        </Grid.ColumnDefinitions>

                        <Grid Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,10">
                            <TextBlock Text="Designfases" FontSize="16" FontWeight="SemiBold" Foreground="#172033"/>
                            <CheckBox HorizontalAlignment="Right" VerticalAlignment="Center"
                                      Content="Alles selecteren"
                                      Style="{StaticResource OptionCheckBoxStyle}"
                                      IsChecked="{Binding ExportIFCViewModel.SelectAll, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>

                        <Border Grid.Row="1" Grid.Column="0" BorderBrush="#E2E8F0" BorderThickness="1" Background="#F8FAFC" CornerRadius="10" Padding="10" Margin="0,0,18,0">
                            <ScrollViewer Height="230" VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding ExportIFCViewModel.Views}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox Content="{Binding ViewName}"
                                                      Style="{StaticResource PhaseCheckBoxStyle}"
                                                      IsChecked="{Binding IsChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Border>

                        <StackPanel Grid.Row="1" Grid.Column="1">
                            <Border Background="#FBF8F1" BorderBrush="#E8DDBF" BorderThickness="1" CornerRadius="10" Padding="15" Margin="0,0,0,12">
                                <StackPanel>
                                    <TextBlock Text="Exportopties" FontWeight="SemiBold" Foreground="#172033" Margin="0,0,0,12"/>
                                    <CheckBox Content="Assemblies exporteren"
                                              Style="{StaticResource OptionCheckBoxStyle}"
                                              IsChecked="{Binding ExportAssemblies, Mode=TwoWay}" Margin="0,0,0,12"/>
                                    <CheckBox Content="IFC-parameters bijwerken"
                                              Style="{StaticResource OptionCheckBoxStyle}"
                                              IsChecked="{Binding UpdateParameters, Mode=TwoWay}"/>
                                </StackPanel>
                            </Border>
                            <Border Background="#F1F5F9" CornerRadius="10" Padding="14">
                                <TextBlock Text="Selecteer minimaal één designfase om de export te starten."
                                           Foreground="#526275" FontSize="12" TextWrapping="Wrap" LineHeight="18"/>
                            </Border>
                        </StackPanel>

                        <TextBlock Grid.Row="2" Grid.ColumnSpan="2" Margin="0,12,0,0"
                                   Text="De gekozen exportinstellingen worden voor een volgende sessie bewaard."
                                   Foreground="#64748B" FontSize="12"/>
                    </Grid>
                </Grid>
            </ScrollViewer>

            <!-- Behoud de oorspronkelijke volgorde van de exporter, met een vaste body zonder buitenste scrollbar. -->
            <Grid Grid.Row="1" Margin="28,18,28,14">
                <Grid.RowDefinitions>
                    <RowDefinition Height="0"/>
                    <RowDefinition Height="52"/>
                    <RowDefinition Height="52"/>
                    <RowDefinition Height="52"/>
                    <RowDefinition Height="46"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.ColumnSpan="3" Visibility="Collapsed"/>

                <TextBlock Grid.Row="1" Text="Exportmap" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center" Margin="0"/>
                <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource RoundedTextBoxStyle}"
                         Text="{Binding FileExportPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Grid.Row="1" Grid.Column="2" Style="{StaticResource SecondaryButtonStyle}" Content="Bladeren" Command="{Binding BrowseCommand}" Width="104" Padding="8,0" Margin="12,0,0,0"/>

                <TextBlock Grid.Row="2" Text="IFC-versie" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center" Margin="0"/>
                <ComboBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Style="{StaticResource RoundedComboBoxStyle}"
                          ItemsSource="{Binding IfcVersions}"
                          SelectedItem="{Binding SelectedIfcVersion, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="3" Text="Coördinatenbasis" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center" Margin="0"/>
                <ComboBox Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" Style="{StaticResource RoundedComboBoxStyle}"
                          ItemsSource="{Binding CoordinateBaseOptions}"
                          SelectedItem="{Binding SelectedCoordinateBase, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="4" Text="Exportopties" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Center" Margin="0"/>
                <StackPanel Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <CheckBox Content="Assemblies exporteren" Style="{StaticResource OptionCheckBoxStyle}"
                              IsChecked="{Binding ExportAssemblies, Mode=TwoWay}" Margin="0,0,42,0"/>
                    <CheckBox Content="IFC-parameters bijwerken" Style="{StaticResource OptionCheckBoxStyle}"
                              IsChecked="{Binding UpdateParameters, Mode=TwoWay}"/>
                </StackPanel>

                <Grid Grid.Row="5" Grid.ColumnSpan="3" Margin="0,8,0,8">
                    <TextBlock Text="Designfase naam" FontSize="14" FontWeight="SemiBold" Foreground="#000000" VerticalAlignment="Center"/>
                    <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Orientation="Horizontal">
                        <CheckBox Content="Alles selecteren" Style="{StaticResource OptionCheckBoxStyle}" Click="SelectAllCheckBox_Click"
                                  IsChecked="{Binding ExportIFCViewModel.SelectAll, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </Grid>

                <Border Grid.Row="6" Grid.ColumnSpan="3" BorderBrush="#DCE3ED" BorderThickness="1" Background="#F8FAFC" CornerRadius="10" Padding="10">
                    <ScrollViewer VerticalScrollBarVisibility="Hidden">
                        <ItemsControl ItemsSource="{Binding ExportIFCViewModel.Views}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Style="{StaticResource PhaseCheckBoxStyle}"
                                              IsChecked="{Binding IsChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                        <Grid>
                                            <TextBlock Text="{Binding ViewName}" VerticalAlignment="Center"/>
                                            <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="11" Foreground="#64748B">
                                                <TextBlock.Text>
                                                    <MultiBinding Converter="{StaticResource PhaseIfcCountConverter}">
                                                        <Binding Path="ViewName"/>
                                                        <Binding Path="PhaseIfcCounts" RelativeSource="{RelativeSource AncestorType={x:Type local:VhExporterWindow}}"/>
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>
                                        </Grid>
                                    </CheckBox>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>
            </Grid>

            <Border Grid.Row="2" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="0,1,0,0" CornerRadius="0,0,13,13" Padding="28,12">
                <Grid>
                    <Button Style="{StaticResource PrimaryButtonStyle}" Content="Exporteren" Click="Export_Click" IsDefault="True" HorizontalAlignment="Right" MinWidth="132"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
