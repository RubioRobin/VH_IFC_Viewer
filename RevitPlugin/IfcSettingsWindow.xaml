<Window x:Class="VH_IFC_QR.IfcSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Instellingen" Height="380" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        FontFamily="Segoe UI" MouseDown="Window_MouseDown">
    
    <Window.Resources>
        <!-- Style for Rounded Buttons -->
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2980B9"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource RoundedButtonStyle}">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#ECF0F1"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style for Rounded TextBoxes -->
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" BorderBrush="#BDC3C7" BorderThickness="1" Background="White" CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#3498DB"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style for Rounded ComboBoxes -->
        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" BorderBrush="#BDC3C7" BorderThickness="1" Background="White"
                                          Focusable="false" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Border" CornerRadius="8" Background="White" BorderBrush="#BDC3C7" BorderThickness="1">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition />
                                                    <ColumnDefinition Width="20" />
                                                </Grid.ColumnDefinitions>
                                                <Path x:Name="Arrow" Grid.Column="1" Fill="#2C3E50" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 4 4 L 8 0 Z"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" 
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" 
                                              Margin="10,0,23,0" VerticalAlignment="Center" HorizontalAlignment="Left" />
                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid x:Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="White" BorderThickness="1" BorderBrush="#BDC3C7" CornerRadius="8"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#F4F7F9" CornerRadius="15" BorderThickness="1" BorderBrush="#D1D8E0">
        <Grid Margin="20,20,20,15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Custom Header / Title Bar Area -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" Margin="0,0,0,15">
                <TextBlock Text="VH IFC &amp; QR Instellingen" FontSize="20" FontWeight="Bold" Foreground="#2C3E50"/>
            </StackPanel>

            <!-- Content -->
            <StackPanel Grid.Row="1">
                <!-- QR Size -->
                <TextBlock Text="QR Code Grootte (mm):" FontWeight="SemiBold" Margin="0,5,0,5"/>
                <TextBox x:Name="txtQrSize" Text="50"/>

                <!-- QR Location -->
                <TextBlock Text="QR Locatie op Sheet:" FontWeight="SemiBold" Margin="0,15,0,5"/>
                <ComboBox x:Name="comboQrLocation">
                    <ComboBoxItem Content="Rechtsonder" Tag="BottomRight"/>
                    <ComboBoxItem Content="Linksonder" Tag="BottomLeft"/>
                    <ComboBoxItem Content="Rechtsboven" Tag="TopRight"/>
                    <ComboBoxItem Content="Linksboven" Tag="TopLeft"/>
                </ComboBox>

                <!-- IFC Version -->
                <TextBlock Text="IFC Versie:" FontWeight="SemiBold" Margin="0,15,0,5"/>
                <ComboBox x:Name="comboIfcVersion">
                    <ComboBoxItem Content="IFC4" Tag="IFC4"/>
                    <ComboBoxItem Content="IFC2x3" Tag="IFC2x3"/>
                    <ComboBoxItem Content="IFC4 RV" Tag="IFC4RV"/>
                </ComboBox>

                <!-- Additional Options -->
                <CheckBox x:Name="chkVisibleOnly" Content="Alleen zichtbare elementen exporteren" Margin="0,20,0,0" IsChecked="True"/>
            </StackPanel>

            <!-- Footer -->
            <UniformGrid Grid.Row="2" Columns="2" Margin="0,15,0,0">
                <Button Content="Annuleren" Click="BtnCancel_Click" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,5,0" IsCancel="True" Height="35"/>
                <Button Content="Opslaan" Click="BtnSave_Click" Style="{StaticResource RoundedButtonStyle}" Margin="5,0,0,0" IsDefault="True" Height="35"/>
            </UniformGrid>
        </Grid>
    </Border>
</Window>
